'use strict';

var objectAssign = require('object-assign'),
    Base = require('../base');

module.exports = objectAssign(Base, {

    // this is the id
    id: 'time-spent',

    start: function () {
        var self = this;


    }

});