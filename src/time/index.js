'use strict';

export default {

    id: 'time-graph',

    init: function () {
        var self = this;

        self.element = document.getElementById(self.id);

        console.debug(self.element);
    }

};